# wws-corpus: source=synthetic (the certbot-managed vhost shape: HTTP-01 webroot location + the ssl_certificate pair certbot writes; parse-clean here is the W4 compat story) license=n/a-synthetic
# wws-corpus: oracle-exit=1 wws-exit=0 delta=oracle-build: pinned oracle has no http_ssl_module and no rewrite module, so `return`/`ssl_certificate` are unknown there (probes p60/p62); stock nginx accepts this config
worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include mime.types;

    server {
        listen 80;
        server_name www.example.org;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl;
        server_name www.example.org;

        ssl_certificate /etc/letsencrypt/live/example.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/example.org/privkey.pem;
        include options-ssl.conf;
        ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

        root /srv/www/site;
        index index.html;
    }
}
