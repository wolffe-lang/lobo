# synapse-docs — ws41: "user" loads with nginx's own warning when lobo is unprivileged,
# so the dry-run predicts. Before ws41 lobo -t refused this config on "user"
# alone (tests/config-corpus/INDEX.md, the blocked table).
args: --request 'GET http://localhost/'
exit: 0
--- expect
== request: GET http://localhost/
decision: no answer — this config has no plaintext listener (every `listen` carries `ssl`) — an http:// request has nowhere to connect
