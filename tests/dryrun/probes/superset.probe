# superset — lobo -t refuses this config (unknown directive "output_buffers" in nginx.conf:92),
# so the dry-run predicts nothing: exit 1, nothing on stdout.
# tests/config-corpus/INDEX.md names what blocks it (ws40).
args: --request 'GET http://localhost/'
exit: 1
--- expect
