# lobo directives — the compat ledger

GENERATED by `tools/lobo-directives` from `src/config/table.lu` — never hand-edit (the gauntlet regenerates and diffs).

Every directive lobo recognizes has a declared row keyed by (name, context). A directive with no row is a load-time error naming the nearest known row. `named_error` rows are the class-C posture: refused BY NAME, never silently ignored, never accidentally inert.

Status counts: **45 implemented** / **50 planned** / **13 named_error** (108 rows).

| directive | contexts | arity | status | note |
|---|---|---|---|---|
| `include` | any | exact(1) | implemented | include graph with globbing, chain-carrying spans, cycle refusal (ws01) |
| `events` | main | exact(0) + block | implemented | context block; connection tuning inside lands ws02 |
| `http` | main | exact(0) + block | implemented | context block; serving lands ws02 |
| `server` | http | exact(0) + block | implemented | virtual-server block; serving lands ws02 |
| `location` | server location | 1..2 + block | implemented | location block, nested allowed; matching semantics land ws02 (regex parse-only) |
| `upstream` | http | exact(1) + block | implemented | upstream pool block; proxying lands ws03 |
| `map` | http | exact(2) + block | implemented | map block; body rows are data entries, evaluation lands ws03 |
| `types` | http server location | exact(0) + block | implemented | MIME map block; body rows are data entries (type ext...), at_least(1) |
| `listen` | server | at_least(1) | implemented | bind address/port; `ssl` flag serves TLS (ws05). Delta, named: ONE plain + ONE ssl listener bind at this pin — additional listen directives parse but do not bind |
| `server_name` | server | at_least(1) | planned(ws02) | host matching |
| `root` | http server location if | exact(1) | implemented | document root (URI appended); location-then-server resolution (ws02) |
| `alias` | location | exact(1) | implemented | path replacement for the matched location part — see LOBO-L003 (ws02) |
| `index` | http server location | at_least(1) | implemented | index files, tried in order on a directory request (ws02) |
| `try_files` | server location | at_least(2) | planned(ws02) | existence chain; last arg is a redirect target, not a file check |
| `error_page` | http server location if | at_least(2) | planned(ws02) | error page mapping |
| `default_type` | http server location | exact(1) | planned(ws02) | MIME type fallback |
| `error_log` | main http server location | 1..3 | implemented | error log path + nginx level ladder (ws09); lobo-native format=json third arg (linted); main/http level applies at v0 |
| `access_log` | http server location if | at_least(1) | implemented | access log path [format] [buffer=] [flush=] | off (ws09); format=json is the lobo-native structured door (linted); no directive means NO log (delta: nginx defaults to logs/access.log); gzip=/if= named post-v1; location-level rows are named-inert at v0 (L005) |
| `log_format` | http | at_least(2) | implemented | named format, escape=default|json|none pinned to the oracle byte-for-byte (ws09); variables per docs/log-variables.md (class-C: an unimplemented variable refuses at load) |
| `log_headers` | http server | at_least(1) | implemented | lobo-native (ws09, linted): request headers the JSON door nests under "headers" |
| `sendfile` | http server location | exact(1) | planned(ws02) | kernel file send |
| `tcp_nopush` | http server location | exact(1) | planned(ws02) | TCP_CORK on sendfile |
| `tcp_nodelay` | http server location | exact(1) | planned(ws02) | TCP_NODELAY on keepalive |
| `keepalive_timeout` | http server location | 1..2 | planned(ws02) | keepalive close timer |
| `keepalive_requests` | http server location | exact(1) | planned(ws02) | keepalive request budget |
| `client_max_body_size` | http server location | exact(1) | implemented | request body cap -> 413; 0 = unlimited (nginx's rule), default 1m (ws10). Location-level rows are named-inert at v0 (L009) — limits resolve at http/server while server selection is host-blind |
| `large_client_header_buffers` | http server | exact(2) | implemented | size bounds the request line (-> 414) and each header line (-> 400 header-too-large); n x size bounds the header block (ws10); default 4 8k |
| `client_header_buffer_size` | http server | exact(1) | planned(ws15) | read-buffer TUNING, not a fence — the hard bounds are large_client_header_buffers's; ws15 (perf) decides whether the small-buffer fast path earns its place |
| `memory_budget` | http server | exact(1) | implemented | lobo-native (ws10, linted L008): per-request admitted-bytes budget -> 503 with a name (nginx's answer to memory pressure is the OOM killer); 0/absent = off. Admission metering AND the runtime's own region cap (ws14, wolf-lang#219 closed): a budgeted body runs inside a per-request proc under a cap of 16 x pow2ceil(the directive) in LEDGER units (D40, ws15 — the envelope follows the ledger's doubling growth), and a breach is a 503 site=region. Set it to the bytes you will admit: since D40 no body the meter admits can cross the cap, so the cap is a BACKSTOP and there is no second arithmetic — docs/BUDGET.md carries the law, the theorem and the measurement |
| `metrics` | http server | exact(1) | implemented | lobo-native (ws12, linted L010/L011): on|off publishes the Prometheus exposition at /metrics and the ws08 status object at /status.json on this server's listener. v0 posture: plaintext step path only, no auth beyond bind-address discipline (ws14 revisits); the endpoint's own admin listen waits on multi-listener support (docs/directives.md) |
| `server_tokens` | http server location | exact(1) | planned(ws02) | version disclosure |
| `etag` | http server location | exact(1) | planned(ws02) | ETag emission |
| `expires` | http server location if | 1..2 | planned(ws02) | Expires/Cache-Control |
| `add_header` | http server location if | 2..3 | planned(ws02) | response header — child presence drops parent set, see LOBO-L002's family |
| `autoindex` | http server location | exact(1) | planned(ws02) | directory listing |
| `charset` | http server location if | exact(1) | planned(ws02) | response charset |
| `open_file_cache` | http server location | 1..2 | planned(ws02) | descriptor cache |
| `worker_connections` | events | exact(1) | planned(ws02) | per-worker connection budget |
| `return` | server location if | 1..2 | planned(ws02) | immediate response/redirect (parse-only at ws01) |
| `gzip` | http server location if | exact(1) | planned(ws02) | on-the-fly compression |
| `gzip_types` | http server location | at_least(1) | planned(ws02) | compressible MIME set |
| `gzip_comp_level` | http server location | exact(1) | planned(ws02) | compression level |
| `gzip_min_length` | http server location | exact(1) | planned(ws02) | compression size floor |
| `gzip_vary` | http server location | exact(1) | planned(ws02) | Vary: Accept-Encoding |
| `gzip_proxied` | http server location | at_least(1) | planned(ws02) | compression for proxied replies |
| `gzip_disable` | http server location | at_least(1) | planned(ws02) | compression opt-out by UA |
| `proxy_pass` | location if | exact(1) | implemented | upstream handoff; URI rewrite per the ws03 probe matrix; https:// rides std.x.tls.client (wsm04 — VERIFIED ALWAYS, proxy_ssl_trusted_certificate required at -t); the variable form is named_error |
| `proxy_ssl_trusted_certificate` | http server location | exact(1) | implemented | trust anchors for proxy_pass https:// (wsm04): concatenated PEM roots the std.x.tls.client verifies the upstream chain+hostname against; REQUIRED for an https upstream (lobo has no unverified mode — nginx's verify-off default is a named delta); path used as written (absolute recommended; nginx resolves prefix-relative — named delta) |
| `proxy_ssl_verify` | http server location | exact(1) | implemented | wsm04: lobo's https upstream leg verifies ALWAYS, so only "on" is accepted — "off" is a named -t refusal (nginx defaults off; the inverted default is a deliberate, documented delta) |
| `proxy_ssl_server_name` | http server location | exact(1) | implemented | wsm04: SNI always rides the https upstream dial (the client's hostname is its verification name), so only "on" is accepted — "off" is a named -t refusal (nginx defaults off; named delta) |
| `proxy_ssl_name` | http server location | exact(1) | implemented | wsm04: overrides the SNI/verification hostname for the https upstream leg (default: the selected peer's host — nginx defaults to the proxy_pass authority, which for a pool is the pool name; the per-peer default is a named delta) |
| `proxy_set_header` | http server location | exact(2) | implemented | upstream request header — child presence drops ALL parent-level ones (LOBO-L002, honored at runtime) |
| `proxy_http_version` | http server location | exact(1) | planned(ws03) | upstream HTTP version |
| `proxy_connect_timeout` | http server location | exact(1) | planned(ws03) | upstream connect deadline |
| `proxy_read_timeout` | http server location | exact(1) | implemented | upstream read deadline (expiry -> 504) |
| `resolver` | http server location | at_least(1) | implemented | ws13: address [valid=time] [ipv4=on|off] [ipv6=on|off]; lobo's own DNS client (TCP transport — nginx tries UDP first, delta) resolves proxy_pass/upstream NAMES at runtime through a TTL cache the ONE poll loop multiplexes (docs/RESOLVER.md). Deltas, named: applies to STATIC proxy_pass names too (nginx resolves those once at load and never again — trac #1064; L013 says so at load), A records only (ipv6=on is accepted and answers nothing; ipv4=off refuses at -t), the first address is used (nginx round-robins several), a failed name is held 1s (nginx re-asks), status_zone= refuses by name |
| `resolver_timeout` | http server location | exact(1) | implemented | ws13: how long a name may take to resolve before the request answers 502 (nginx's default 30s); the query is abandoned, the failure held 1s (docs/RESOLVER.md) |
| `proxy_send_timeout` | http server location | exact(1) | planned(ws03) | upstream send deadline |
| `proxy_buffering` | http server location | exact(1) | planned(ws03) | reply buffering |
| `proxy_redirect` | http server location | 1..2 | planned(ws03) | Location rewrite |
| `proxy_cache` | http server location | exact(1) | planned(ws03) | reply cache zone |
| `proxy_cache_valid` | http server location | at_least(1) | planned(ws03) | cache TTLs |
| `server` | upstream | at_least(1) | implemented | pool member address and flags (weight/max_fails/fail_timeout/backup); smooth weighted round-robin, probed against the oracle |
| `keepalive` | upstream | exact(1) | planned(ws03) | idle upstream connections |
| `least_conn` | upstream | exact(0) | planned(ws03) | balancing method |
| `ip_hash` | upstream | exact(0) | planned(ws03) | balancing method |
| `fastcgi_pass` | location if | exact(1) | planned(ws03) | FastCGI handoff (php-fpm shape parses at ws01) |
| `fastcgi_param` | http server location | 2..3 | planned(ws03) | FastCGI parameter |
| `fastcgi_index` | http server location | exact(1) | planned(ws03) | FastCGI index file |
| `fastcgi_split_path_info` | location | exact(1) | planned(ws03) | PATH_INFO split (regex parse-only) |
| `upstream_keepalive_timeout` | upstream | exact(1) | planned(ws03) | idle upstream close timer |
| `rewrite` | server location if | 2..3 | planned(ws03) | regex rewrite (parse-only at ws01) |
| `set` | server location if | exact(2) | planned(ws03) | variable assignment (syntax only at ws01) |
| `if` | server location | at_least(1) + block | planned(ws03) | conditional block — see LOBO-L001 before using inside location |
| `worker_processes` | main | exact(1) | implemented | worker task-group count (delta: lobo maps workers to supervised task-groups in ONE process, not process-per-worker — crash isolation is the supervision tree; ws14 revisits process isolation) |
| `worker_priority` | main | exact(1) | named_error | single-process task model — no per-worker os process to renice (ws14 revisits) |
| `worker_rlimit_nofile` | main | exact(1) | named_error | single-process task model — one descriptor budget, not per-worker (ws14 revisits) |
| `worker_shutdown_timeout` | main | exact(1) | implemented | drain deadline: a reloaded generation drains its held connections, and this is how long before the remainder are aborted and it retires anyway (ws08; nginx's semantics — 0/absent means drain fully, no forced timeout) |
| `pid` | main | exact(1) | implemented | pid file (delta: at this pin the file records the control endpoint, not an os pid — the pin exposes no getpid/signal; #126 flips it to the os pid) |
| `user` | main | 1..2 | named_error | privilege drop is ws14's — run lobo as the target user for now (nginx only warns when unprivileged) |
| `daemon` | main | exact(1) | named_error | foreground-only v0 (systemd/container reality; the oracle runs daemon off in every harness case) |
| `master_process` | main | exact(1) | named_error | foreground-only v0 — the master/worker split is the supervision tree, not an os toggle |
| `control` | main | 1..3 | implemented | lobo-native (ws04, widened at ws15): the loopback endpoint a running lobo takes orders on — reload/quit/stop/reopen/status/upgrade/ping, the SAME dispatch a real signal drives. `control <addr>` or `control <addr> token <file>`; off by default. The listener is loopback TCP on EVERY host: wolf has no unix-domain socket at this pin (measured, wolf-lang#227), so the filesystem-permissioned socket every comparable server uses is unavailable and the optional token file is the auth arm instead — lobo READS it, never writes it (std has no permission surface, wolf-std#5). NOT an nginx directive (the oracle -t rejects it, a named delta); nginx's own trigger is kill(pid, SIGHUP) and the kernel authorizes it — docs/CONTROL.md's delta table |
| `ssl_certificate` | http server | exact(1) | implemented | fullchain PEM, leaf-first enforced with a named reorder error; Ed25519 leaf only at this pin (RSA/ECDSA are std.x.tls roadmap rows, refused BY NAME at -t, never accepted silently) — existing layouts keep working (charter hard minimum, ws05) |
| `ssl_certificate_key` | http server | exact(1) | implemented | PKCS#8 PEM; pair MATCH verified at -t by public-key comparison; Ed25519 only at this pin (named refusal otherwise); encrypted keys are a named delta (ws05) |
| `ssl_protocols` | http server | at_least(1) | implemented | v0 honest set: TLSv1.3 implemented; TLSv1.2 listed = config-time named delta citing the std.x.tls ladder (1.2 is its roadmap row); a set without TLSv1.3 is a config error (ws05) |
| `ssl_ciphers` | http server | exact(1) | implemented | TLS 1.3 suites mapped: TLS_CHACHA20_POLY1305_SHA256 implemented (the RFC 8446 MTI), AES-GCM pair named roadmap, unknown TLS_* = named error listing supported; 1.2 cipher-list tokens are inert for 1.3 (nginx parity, warned) (ws05) |
| `ssl_prefer_server_ciphers` | http server | exact(1) | planned(wsc02) | cipher preference |
| `ssl_session_cache` | http server | 1..2 | planned(wsc02) | session cache — TLS 1.3 resumption is a NAMED std.x.tls roadmap row (ws05 serves full handshakes only) |
| `ssl_session_timeout` | http server | exact(1) | planned(wsc02) | session TTL — rides the resumption roadmap row (ws05 note) |
| `ssl_dhparam` | http server | exact(1) | planned(wsc02) | DH parameters |
| `ssl_stapling` | http server | exact(1) | planned(wsc02) | OCSP stapling |
| `ssl_stapling_verify` | http server | exact(1) | planned(wsc02) | OCSP verification |
| `cert` | server | exact(1) | implemented | lobo-native (ws06): "cert auto" — the server acquires, renews and rotates its own certificate via ACME HTTP-01; the certbot dance keeps working beside it (coexistence is the posture). NOT an nginx directive (L004 one-way door) |
| `cert_contact` | http server | exact(1) | implemented | lobo-native (ws06): ACME account contact (a mailto: URL or bare address) sent at account creation |
| `cert_ca` | http server | exact(1) | implemented | lobo-native (ws06; https at wsm04): the ACME directory URL — loopback http:// or https:// (the https dial rides std.x.tls.client, verified against cert_ca_root). LOOPBACK ONLY forever: a non-loopback URL is a named -t refusal (the loopback law) |
| `cert_ca_root` | http server | exact(1) | implemented | lobo-native (wsm04): the trust-anchor PEM for an https cert_ca — the ACME client verifies the directory's chain against it ALWAYS (required when cert_ca is https://; the harness CA writes its root out for exactly this) |
| `cert_store` | http server | exact(1) | implemented | lobo-native (ws06): where acquired certificates live (default: <prefix>/certs) — per-domain dirs, account key separate, every artifact written atomically (write+rename) |
| `cert_renew_before` | http server | exact(1) | implemented | lobo-native (ws06): renew when the certificate has less than this long to live (nginx time units; default 30d) |
| `cert_check_interval` | http server | exact(1) | implemented | lobo-native (ws06): how often the renewal daemon wakes to check expiry (nginx time units; default 1d — sub-daily values are the harness's, not production advice) |
| `content_by_lua_block` | location if | exact(0) + block | named_error | OpenResty Lua — lobo embeds no Lua; block skipped by token-level brace count (a } inside a Lua comment or long string defeats the skip) |
| `access_by_lua_block` | http server location if | exact(0) + block | named_error | OpenResty Lua — lobo embeds no Lua; block skipped by token-level brace count |
| `init_by_lua_block` | http | exact(0) + block | named_error | OpenResty Lua — lobo embeds no Lua; block skipped by token-level brace count |
| `rewrite_by_lua_block` | http server location if | exact(0) + block | named_error | OpenResty Lua — lobo embeds no Lua; block skipped by token-level brace count |
| `lua_package_path` | http | exact(1) | named_error | OpenResty Lua — lobo embeds no Lua |
| `lua_shared_dict` | http | exact(2) | named_error | OpenResty Lua — lobo embeds no Lua |
| `perl` | location if | exact(1) | named_error | embedded Perl — out of scope permanently |
| `perl_modules` | http | exact(1) | named_error | embedded Perl — out of scope permanently |
