# lobo — CI (ws00; repaired at ws12). Committed BEFORE any remote
# existed, so it had never run — and had silently rotted twice: the
# gauntlet was renamed at wsm03 (`wws-gauntlet` -> `lobo-gauntlet`)
# and a THIRD pinned artifact arrived at wsm04 (`[std]`, a source tree
# staged into .wolf-bin/std with its STD-REV marker), neither of which
# reached this file. ws12 repairs both, renames the checkout path to
# match the repo, and adds the host tools the rig leans on. It is
# written against the pins so the first real run is green or honestly
# red. Conventions mirrored from the wolf-lang/wolf-book CI: explicit
# shell, timeout ceilings, loud SKIPs (a lane that cannot run says so;
# it never passes vacuously). linux x86-64 first; the matrix grows
# when the repo goes public. Loopback only — nothing here touches an
# external network except fetching the pinned sources.

name: ci

on:
  push:
    branches: [trunk]
  pull_request:
  workflow_dispatch:
    inputs:
      parity:
        description: "ws22: run tools/lobo-parity (the W8 bar, docs/PARITY.md) INSTEAD of the gauntlet — a number on linux x86-64, never a gate; the table is the step's log"
        type: boolean
        default: false
      ref_tree:
        description: "ws25 (lobo#6): a SECOND lobo ref (branch, tag or sha) built beside this one with this checkout's toolchain; with parity=true the set is three fresh servers per pair on ONE VM and the table carries this ÷ ref against the VM's own nginx"
        type: string
        default: ""
      ref_name:
        description: "ws25: the column label for ref_tree (default: ref)"
        type: string
        default: "ref"
      this_name:
        description: "ws25: the column label for this checkout (default: lobo)"
        type: string
        default: "lobo"
      profile:
        description: "ws25 (lobo#6): the profile leg — tools/lobo-profile (perf record + strace -c on one hand under the close shape) on this checkout, and on ref_tree when given; runs after the parity set, or alone"
        type: boolean
        default: false
      profile_shape:
        description: "ws27: the shape the profile leg drives — close (ws22's probe, the default) or keepalive"
        type: string
        default: "close"
      syscalls:
        description: "ws27: the count leg — tools/lobo-syscalls (strace -c over BOTH servers for a whole drive, calls per request side by side) at N=nproc on both shapes; runs after the parity set and the profile leg, or alone"
        type: boolean
        default: false

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  gauntlet:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          path: lobo
          fetch-depth: 0   # the stamp step holds the channel against the tags (ws19)

      - name: read toolchain pins
        id: pins
        shell: bash
        working-directory: lobo
        run: |
          rev() { awk -v s="[$1]" '$0==s{f=1} f&&/^rev = /{gsub(/"/,"",$3); print $3; exit}' wolf-toolchain.toml; }
          echo "wolf=$(rev wolf)" >> "$GITHUB_OUTPUT"
          echo "lupin=$(rev lupin)" >> "$GITHUB_OUTPUT"
          echo "std=$(rev std)" >> "$GITHUB_OUTPUT"

      # The sibling repos are private; the default token cannot read
      # them (the wolf-book lesson). With WOLF_CI_TOKEN the job runs
      # in full; without it every toolchain step below SKIPs loudly.
      - name: acquire pinned siblings
        id: acquire
        shell: bash
        env:
          TOKEN: ${{ secrets.WOLF_CI_TOKEN }}
        run: |
          set -uo pipefail
          if [ -z "${TOKEN:-}" ]; then
            echo "::notice::WOLF_CI_TOKEN absent — sibling repos unreachable; the gauntlet lane skips (local runs remain the gate)"
            echo "toolchain=none" >> "$GITHUB_OUTPUT"; exit 0
          fi
          ok=1
          git clone --quiet "https://x-access-token:${TOKEN}@github.com/wolffe-lang/wolf-lang" wolf-lang \
            && git -C wolf-lang checkout --quiet "${{ steps.pins.outputs.wolf }}" || ok=0
          git clone --quiet "https://x-access-token:${TOKEN}@github.com/wolffe-lang/wolf-interp" wolf-interp \
            && git -C wolf-interp checkout --quiet "${{ steps.pins.outputs.lupin }}" || ok=0
          git clone --quiet "https://x-access-token:${TOKEN}@github.com/wolffe-lang/wolf-std" wolf-std || ok=0
          if [ "$ok" = 1 ]; then echo "toolchain=full" >> "$GITHUB_OUTPUT"
          else echo "::error::token present but sibling acquisition failed"; exit 1; fi

      - uses: Swatinem/rust-cache@v2
        if: steps.acquire.outputs.toolchain == 'full'
        with:
          workspaces: |
            wolf-lang
            wolf-interp

      - name: build pinned wolf (release) + runtime staticlib
        if: steps.acquire.outputs.toolchain == 'full'
        shell: bash
        working-directory: wolf-lang
        run: |
          set -euo pipefail
          # The stamp is used VERBATIM in `--version`, and
          # wolf-toolchain.toml's version_line names the SHORT rev
          # (the D57 shape sc30 recorded), so truncate here rather
          # than letting the identity check refuse a full-sha stamp.
          # ws13: when the pinned rev IS a release tag (v0.2.2 was the
          # first), the D57 release stamp rides along — xtask's own
          # rule, granted only when that exact tag points at HEAD —
          # so `--version` claims the bare version the pin file names.
          # A trunk pin has no such tag and stays `+dev.<short>`.
          TAG="$(git tag --points-at HEAD | grep '^v[0-9]' | head -1 || true)"
          if [ -n "$TAG" ]; then export WOLF_RELEASE="$TAG"; fi
          WOLF_COMMIT="$(echo "${{ steps.pins.outputs.wolf }}" | cut -c1-7)" \
            cargo build --release -p wolf_driver -p wolf_rt

      - name: build pinned lupin (release)
        if: steps.acquire.outputs.toolchain == 'full'
        shell: bash
        working-directory: wolf-interp
        run: cargo build --release --bin lupin

      # The wolf pin is built with the D57 dev stamp: WOLF_COMMIT is
      # what makes `--version` claim the rev, and lib-toolchain.sh
      # refuses the unstamped `+dev.unknown` a plain cargo build emits.
      - name: stage .wolf-bin
        if: steps.acquire.outputs.toolchain == 'full'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p lobo/.wolf-bin
          cp wolf-lang/target/release/wolf wolf-lang/target/release/libwolf_rt.a lobo/.wolf-bin/
          cp wolf-interp/target/release/lupin lobo/.wolf-bin/
          # [std] is a SOURCE TREE, not a binary: staged by the
          # documented one-time action, identified by its STD-REV
          # marker (a tree has no --version).
          git -C wolf-std archive "${{ steps.pins.outputs.std }}" std | tar -x -C lobo/.wolf-bin/
          git -C wolf-std rev-parse "${{ steps.pins.outputs.std }}" > lobo/.wolf-bin/std/STD-REV

      # The differential oracle, cached on the pin file — a CI step is
      # not a build script in the D33 sense (the wsc00 closeout records
      # the interpretation). The build recipe is docs/DIFFERENTIAL.md's,
      # driven by the values in NGINX-PIN itself.
      - name: cache pinned nginx
        if: steps.acquire.outputs.toolchain == 'full'
        id: nginx-cache
        uses: actions/cache@v4
        with:
          path: lobo/tests/differential/bin
          key: nginx-${{ runner.os }}-${{ hashFiles('lobo/tests/differential/NGINX-PIN') }}

      - name: build pinned nginx (cache miss only)
        if: steps.acquire.outputs.toolchain == 'full' && steps.nginx-cache.outputs.cache-hit != 'true'
        shell: bash
        working-directory: lobo
        run: |
          set -euo pipefail
          PIN="$PWD/tests/differential/NGINX-PIN"
          pin() { sed -n "s/^$1 = //p" "$PIN"; }
          mkdir -p /tmp/nginx-build && cd /tmp/nginx-build
          curl -sSLO "$(pin source)"
          echo "$(pin sha256)  $(pin tarball)" | sha256sum -c -
          tar xzf "$(pin tarball)"
          cd "nginx-$(pin version)"
          $(pin configure)
          make -j"$(nproc)"
          mkdir -p "$GITHUB_WORKSPACE/lobo/tests/differential/bin"
          cp objs/nginx "$GITHUB_WORKSPACE/lobo/tests/differential/bin/nginx"
          rm -rf /tmp/nginx-build

      # The rig's host tools beyond the toolchain (CLAUDE.md's list):
      # POSIX sh, awk, jq, diff and a real OpenSSL 3.x for the TLS
      # steps, plus nc for the acme/proxy/metrics drives. ubuntu-latest
      # ships awk/jq/diff/openssl-3.x; netcat is the only add.
      - name: host tools
        if: steps.acquire.outputs.toolchain == 'full'
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -qq
          sudo apt-get install -y -qq netcat-openbsd
          openssl version

      - name: lobo-gauntlet
        if: steps.acquire.outputs.toolchain == 'full' && inputs.parity != true && inputs.profile != true && inputs.syscalls != true
        shell: bash
        working-directory: lobo
        run: tools/lobo-gauntlet

      # ws22: the W8 bar on the one linux x86-64 the org has hands on.
      # docs/PARITY.md names this runner as a gating HOST and its noise
      # as the reason the statistic is a same-box ratio; a refused set
      # (load, the generator as ceiling, an unstable oracle) is a red
      # step with the reason in the log, never a vacuous green.
      # ws25 (lobo#6): a second tree, built beside this one with the
      # SAME staged toolchain, so the parity set (and the profile leg)
      # can read two lobos on ONE VM against that VM's own nginx — the
      # only linux delta the runner can hold still. The ref tree's own
      # pin file must name the same wolf (its toolchain library refuses
      # otherwise, which is the right answer).
      - name: "build the ref tree (ws25)"
        if: steps.acquire.outputs.toolchain == 'full' && (inputs.parity == true || inputs.profile == true) && inputs.ref_tree != ''
        shell: bash
        run: |
          set -euo pipefail
          git -C lobo worktree add ../lobo-ref "${{ inputs.ref_tree }}"
          ln -s "$PWD/lobo/.wolf-bin" lobo-ref/.wolf-bin
          ln -s "$PWD/lobo/tests/differential/bin" lobo-ref/tests/differential/bin
          cd lobo-ref
          echo "ref tree: $(git rev-parse --short HEAD) $(git log -1 --format=%s | cut -c1-90)"
          mkdir -p target
          sh -c '. tools/lib-toolchain.sh; WOLF_MIDEND=0 "$WOLF" build --release ./src/main.lu -o target/lobo-release'
          ./target/lobo-release -v

      - name: lobo-parity (the W8 bar — a number, never a gate)
        if: steps.acquire.outputs.toolchain == 'full' && inputs.parity == true
        shell: bash
        working-directory: lobo
        env:
          LOBO_REF: ${{ inputs.ref_tree != '' && format('{0}/lobo-ref/target/lobo-release', github.workspace) || '' }}
          LOBO_REF_NAME: ${{ inputs.ref_name }}
          LOBO_NAME: ${{ inputs.this_name }}
        run: |
          set -euo pipefail
          sudo apt-get install -y -qq apache2-utils
          mkdir -p target
          sh -c '. tools/lib-toolchain.sh; WOLF_MIDEND=0 "$WOLF" build --release ./src/main.lu -o target/lobo-release'
          # the bar's quiet-rig rule (load(1m) under 3.0) — the build
          # that just finished is the runner's own load; let it decay,
          # bounded, and let the tool refuse if it does not
          for i in $(seq 1 18); do
            l=$(cut -d' ' -f1 /proc/loadavg); echo "load(1m) $l"
            awk -v l="$l" 'BEGIN { exit (l < 2.0) ? 0 : 1 }' && break
            sleep 10
          done
          nproc; uptime
          [ -z "$LOBO_REF" ] || echo "two trees: $LOBO_NAME = $PWD/target/lobo-release, $LOBO_REF_NAME = $LOBO_REF"
          tools/lobo-parity

      # ws25 (lobo#6): the profile leg. perf needs the kernel's own
      # linux-tools; the azure kernel's package is in the archive
      # under its exact release, and when it is not the tool SKIPS by
      # name (a skip is not a number). strace is the syscall counter.
      - name: "lobo-profile (ws25 — perf record + strace -c on one hand, close shape)"
        if: steps.acquire.outputs.toolchain == 'full' && inputs.profile == true
        shell: bash
        working-directory: lobo
        run: |
          set -uo pipefail
          sudo apt-get install -y -qq apache2-utils strace > /dev/null
          sudo apt-get install -y -qq linux-tools-common "linux-tools-$(uname -r)" > /dev/null 2>&1 \
            || sudo apt-get install -y -qq linux-tools-common linux-tools-generic > /dev/null 2>&1 \
            || echo "::warning::no linux-tools package for $(uname -r); the profile leg will skip by name"
          if ! command -v perf > /dev/null 2>&1; then
            p=$(ls -d /usr/lib/linux-tools/*/perf 2>/dev/null | head -1 || true)
            [ -n "$p" ] && sudo ln -sf "$p" /usr/local/bin/perf
          fi
          perf --version || true
          sudo sysctl -w kernel.perf_event_paranoid=-1 kernel.kptr_restrict=0 > /dev/null || true
          mkdir -p target
          [ -x target/lobo-release ] || sh -c '. tools/lib-toolchain.sh; WOLF_MIDEND=0 "$WOLF" build --release ./src/main.lu -o target/lobo-release'
          nproc; uptime
          tools/lobo-profile target/lobo-release "${{ inputs.this_name }}" 8 "${{ inputs.profile_shape }}"
          if [ -n "${{ inputs.ref_tree }}" ]; then
            tools/lobo-profile "$GITHUB_WORKSPACE/lobo-ref/target/lobo-release" "${{ inputs.ref_name }}" 8 "${{ inputs.profile_shape }}"
          fi

      # ws27: the count leg. strace -c over every serving process of
      # BOTH servers for a whole drive, the calls column divided by the
      # requests the generators completed — a count is the one linux
      # number a shared VM holds still, and it is taken at the bar's
      # own cell (N=nproc, c=32) on both shapes.
      - name: "lobo-syscalls (ws27 — strace -c over both servers, calls per request, both shapes)"
        if: steps.acquire.outputs.toolchain == 'full' && inputs.syscalls == true
        shell: bash
        working-directory: lobo
        run: |
          set -uo pipefail
          sudo apt-get install -y -qq apache2-utils strace > /dev/null
          mkdir -p target
          [ -x target/lobo-release ] || sh -c '. tools/lib-toolchain.sh; WOLF_MIDEND=0 "$WOLF" build --release ./src/main.lu -o target/lobo-release'
          nproc; uptime
          tools/lobo-syscalls keepalive
          tools/lobo-syscalls close
