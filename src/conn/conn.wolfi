module pkg :: conn
  wolfi v0 · toolchain 0.2.11 · edition v1
  export_hash e541dc86e07ff6c9c2b3e01dee1b4ea043991a5f05babdc240ac728786a3c2df
  pkg_hash    e541dc86e07ff6c9c2b3e01dee1b4ea043991a5f05babdc240ac728786a3c2df
  deps:
    tls  f2fb6d355389a56f1d7f864697aa93726081f938bd67020205d676ea64871ecf
  items:
    [0] pub conn_close — fn conn_close(mut cn: self.Conn) · regions (ρ1) -> -
    [1] pub conn_deadline — fn conn_deadline(cn: self.Conn, ms: int) -> () ! {io} · regions (ρ1, -) -> -
    [2] pub conn_read — fn conn_read(mut cn: self.Conn, max: int) -> str ! {closed, io, timeout, utf8} · regions (ρ1, -) -> -
    [3] pub conn_read_bytes — fn conn_read_bytes(mut cn: self.Conn, max: int) -> prelude.List[byte] ! {closed, io, timeout} · regions (ρ1, -) -> ρ_caller
    [4] pub conn_write — fn conn_write(mut cn: self.Conn, s: str) -> () ! {closed, io} · regions (ρ1, -) -> -
    [5] pub conn_write_bytes — fn conn_write_bytes(mut cn: self.Conn, body: prelude.List[byte]) -> () ! {closed, io} · regions (ρ1, ρ2) -> -
    [6] pub fd — fn fd(cn: self.Conn) -> int · regions (ρ1) -> -
    [7] pub is_tls — fn is_tls(cn: self.Conn) -> bool · regions (ρ1) -> -
    [8] pub plain — fn plain(fd: int) -> self.Conn · regions (-) -> ρ_caller
    [9] pub sni — fn sni(cn: self.Conn) -> str · regions (ρ1) -> -
    [10] pub tls_conn — fn tls_conn(fd: int, sess: tls.Sess) -> self.Conn · regions (-, ρ1) -> ρ_caller
    [11] pub truncated — fn truncated(cn: self.Conn) -> bool · regions (ρ1) -> -
    [12] pub Conn — struct Conn { kind: int, fd: int, sess: tls.Sess }

