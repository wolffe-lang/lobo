module pkg :: serve
  wolfi v0 · toolchain 0.2.8 · edition v1
  export_hash b3740f3d19af032a0969cfac4783791abd6750bf9d77e0c221928914d04faf4d
  pkg_hash    b3740f3d19af032a0969cfac4783791abd6750bf9d77e0c221928914d04faf4d
  deps:
    budget  1c41c0afe015bb4bc622d9d73d17f7a82e8408ed979f05f0a509427c4cc3a535
    config  fe1a5f8e7a3a66678b13f4c0eae28fe55f3403b9ee4a779b779a710087b0111c
    conn  a8dce48cfb72135361db68214a3e5b84192eb97bc6dd277495bde6d78dc68c8f
    http  e87ea50e617d19c43dae3ce6fe36266fec54f74f9c393a61b1f7cc151625ad80
    metrics  49975c8dd41ef069fb77d1b3f5f138d4e2f852b91fbf913afe1f01d9e52df616
    obs  c62583017fcfb635886dd75940b314283288fecb483d047e265f2a31f03b65a8
    proxy  42ee542a973ec86d40f2ab5a327561a9e6550ac295ac40ad9c97749e17216937
    resolver  addc0a3a214e8cff5ad322209a2a981d6999e7de0b5b963d0964b21535afb586
  items:
    [0] pub accept_loop — fn accept_loop(c: config.Conf, addr: str, opts: self.ServerOpts, limits: http.Limits) -> () ! {io} · regions (ρ1, -, ρ2, ρ3) -> -
    [1] pub admin_err_len — fn admin_err_len(token: str) -> int
    [2] pub admin_kind — fn admin_kind(on: bool, path: str) -> str
    [3] pub carry_has_head — fn carry_has_head(carry: str) -> bool
    [4] pub conn_idle — fn conn_idle(carry: str, served: int) -> self.ConnStep · regions (-, -) -> ρ_caller
    [5] pub conn_step — fn conn_step(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, carry: str, served: int, poll_ms: int, mut rs: resolver.Resolver) -> self.ConnStep · regions (-, ρ1, ρ2, ρ3, -, -, -, -, ρ4) -> ρ_caller
    [6] pub default_opts — fn default_opts() -> self.ServerOpts · regions () -> ρ_caller
    [7] pub error_body — fn error_body(code: int, server: str) -> str
    [8] pub error_response — fn error_response(code: int, special_header_body: bool, date: str, token: str) -> str
    [9] pub header_too_large_body — fn header_too_large_body(server: str) -> str
    [10] pub http_date — fn http_date(secs: int) -> str
    [11] pub ledger_cap — fn ledger_cap(budget: int) -> int
    [12] pub listen_addr — fn listen_addr(c: config.Conf) -> str · regions (ρ1) -> -
    [13] pub listen_addr_plain — fn listen_addr_plain(c: config.Conf) -> str · regions (ρ1) -> -
    [14] pub mime_for — fn mime_for(c: config.Conf, path: str) -> str · regions (ρ1, -) -> -
    [15] pub open_self_pipe — fn open_self_pipe() -> (int, int) ! {io} · regions () -> ρ_caller
    [16] pub raw_line — fn raw_line(head: str) -> str
    [17] pub resolve_limits — fn resolve_limits(c: config.Conf) -> http.Limits · regions (ρ1) -> ρ_caller
    [18] pub serve_conn — fn serve_conn(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int) · regions (-, ρ1, ρ2, ρ3, -) -> -
    [19] pub serve_conn_cn — fn serve_conn_cn(mut cn: conn.Conn, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, mut rs: resolver.Resolver) · regions (ρ1, ρ2, ρ3, ρ4, -, ρ5) -> -
    [20] pub serve_request — fn serve_request(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, head: str, rest0: str, served: int, mut rs: resolver.Resolver) -> self.ReqOut · regions (-, ρ1, ρ2, ρ3, -, -, -, -, ρ4) -> ρ_caller
    [21] pub status_phrase — fn status_phrase(code: int) -> str
    [22] pub to_hex — fn to_hex(n: int) -> str
    [23] pub version — fn version() -> str
    [24] pub write_admin — fn write_admin(conn: int, body: str, ctype: str, token: str, keep: bool) -> int ! {closed, io}
    [25] pub write_admin_503 — fn write_admin_503(conn: int, token: str)
    [26] pub ConnStep — struct ConnStep { active: bool, close: bool, carry: str, served: int, logged: bool, acc: obs.Access, bud_site: str, bud_limit: int, bud_would: int, bud_cap: int, mem_hw: int, mem_rt: int, admin: str, resolve: str, resolve_addr: str, resolve_timeout_ms: int, resolve_valid_ms: int }
    [27] pub ReqOut — struct ReqOut { close: bool, carry: str, logged: bool, acc: obs.Access, bud_site: str, bud_limit: int, bud_would: int, bud_cap: int, mem_hw: int, mem_rt: int, admin: str, resolve: str, resolve_addr: str, resolve_timeout_ms: int, resolve_valid_ms: int }
    [28] pub ServerOpts — struct ServerOpts { token: str, keepalive_timeout_ms: int, keepalive_requests: int, read_timeout_ms: int, challenge_dir: str, metrics_on: bool, pipe_w: int, pipe_r: int }

