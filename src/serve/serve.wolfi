module pkg :: serve
  wolfi v0 · toolchain 0.2.12 · edition v1
  export_hash a516eb7587c2dd596403ca0a18d1ba18f458fbe8e7a540ef98d0a31dc8c60e8c
  pkg_hash    a516eb7587c2dd596403ca0a18d1ba18f458fbe8e7a540ef98d0a31dc8c60e8c
  deps:
    budget  9299e00a4af3ebec20e314be715603c6490f169af63de01461fb714934914390
    config  a166b26acb6cf67ec86f30b21fcc0726661d53f5928c54b4fa2ed065a7dc500f
    conn  55fd3a00523026ad863598c45959b6de9e1be4a5cb37ed692bcf00b5cccd61cb
    http  c8f7d6fed0d5bfbdbea55aa77396a6af4f946bcb0414e49e6130041d50319ea9
    metrics  ba75b3743f76d035354c66ad9c0024b5da658383233e56e8a421b9044ae4dfe8
    obs  c978bb5286100f8729ffcee6bee9f27526125205ed6bc308aae12b991eb22e32
    proxy  afad3fd7c33c91fbe5ce902c118cfc7bdf8fa610cdcab9a0042b5efc9bab0d45
    resolver  f529fb7456b2b0154944edba97cca1c42121514b47a38dc5bf9b6ddc89132caa
  items:
    [0] pub accept_loop — fn accept_loop(c: config.Conf, addr: str, opts: self.ServerOpts, limits: http.Limits) -> () ! {io} · regions (ρ1, -, ρ2, ρ3) -> -
    [1] pub admin_err_len — fn admin_err_len(token: str) -> int
    [2] pub admin_kind — fn admin_kind(on: bool, path: str) -> str
    [3] pub carry_has_head — fn carry_has_head(carry: str) -> bool
    [4] pub classify — fn classify(path: str) -> self.Classified · regions (-) -> ρ_caller
    [5] pub conn_idle — fn conn_idle(carry: str, served: int) -> self.ConnStep · regions (-, -) -> ρ_caller
    [6] pub conn_step — fn conn_step(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, carry: str, served: int, poll_ms: int, mut rs: resolver.Resolver, mut ft: self.FileKinds) -> self.ConnStep · regions (-, ρ1, ρ2, ρ3, -, -, -, -, ρ4, ρ5) -> ρ_caller
    [7] pub date_str — fn date_str(mut ft: self.FileKinds, secs: int) -> str · regions (ρ1, -) -> -
    [8] pub default_opts — fn default_opts() -> self.ServerOpts · regions () -> ρ_caller
    [9] pub error_body — fn error_body(code: int, server: str) -> str
    [10] pub error_response — fn error_response(code: int, special_header_body: bool, date: str, token: str) -> str
    [11] pub file_kinds_new — fn file_kinds_new() -> self.FileKinds · regions () -> ρ_caller
    [12] pub head_remember — fn head_remember(mut ft: self.FileKinds, path: str, size: int, mtime_ms: int, keep: bool, secs: int, token: str, head: str) · regions (ρ1, -, -, -, -, -, -, -) -> -
    [13] pub head_warm — fn head_warm(ft: self.FileKinds, path: str, size: int, mtime_ms: int, keep: bool, secs: int, token: str) -> str · regions (ρ1, -, -, -, -, -, -) -> -
    [14] pub header_too_large_body — fn header_too_large_body(server: str) -> str
    [15] pub http_date — fn http_date(secs: int) -> str
    [16] pub ledger_cap — fn ledger_cap(budget: int) -> int
    [17] pub listen_addr — fn listen_addr(c: config.Conf) -> str · regions (ρ1) -> -
    [18] pub listen_addr_plain — fn listen_addr_plain(c: config.Conf) -> str · regions (ρ1) -> -
    [19] pub listen_reuseport — fn listen_reuseport(c: config.Conf) -> bool · regions (ρ1) -> -
    [20] pub mime_for — fn mime_for(c: config.Conf, path: str) -> str · regions (ρ1, -) -> -
    [21] pub open_self_pipe — fn open_self_pipe() -> (int, int) ! {io} · regions () -> ρ_caller
    [22] pub raw_line — fn raw_line(head: str) -> str
    [23] pub resolve_limits — fn resolve_limits(c: config.Conf) -> http.Limits · regions (ρ1) -> ρ_caller
    [24] pub serve_conn — fn serve_conn(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int) · regions (-, ρ1, ρ2, ρ3, -) -> -
    [25] pub serve_conn_cn — fn serve_conn_cn(mut cn: conn.Conn, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, mut rs: resolver.Resolver) · regions (ρ1, ρ2, ρ3, ρ4, -, ρ5) -> -
    [26] pub serve_request — fn serve_request(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, head: str, rest0: str, served: int, mut rs: resolver.Resolver, mut ft: self.FileKinds) -> self.ReqOut · regions (-, ρ1, ρ2, ρ3, -, -, -, -, ρ4, ρ5) -> ρ_caller
    [27] pub status_phrase — fn status_phrase(code: int) -> str
    [28] pub to_hex — fn to_hex(n: int) -> str
    [29] pub version — fn version() -> str
    [30] pub write_admin — fn write_admin(conn: int, body: str, ctype: str, token: str, keep: bool) -> int ! {closed, io}
    [31] pub write_admin_503 — fn write_admin_503(conn: int, token: str)
    [32] pub Classified — struct Classified { kind: int, fd: int, size: int, mtime_ms: int }
    [33] pub ConnStep — struct ConnStep { active: bool, close: bool, carry: str, served: int, logged: bool, acc: obs.Access, bud_site: str, bud_limit: int, bud_would: int, bud_cap: int, mem_hw: int, mem_rt: int, admin: str, resolve: str, resolve_addr: str, resolve_timeout_ms: int, resolve_valid_ms: int }
    [34] pub FileKinds — struct FileKinds { hpath: prelude.List[str], hsize: prelude.List[int], hmtime: prelude.List[int], hsecs: prelude.List[int], hkeep: prelude.List[int], htoken: prelude.List[str], hhead: prelude.List[str], date_secs: int, date_str: str }
    [35] pub ReqOut — struct ReqOut { close: bool, carry: str, logged: bool, acc: obs.Access, bud_site: str, bud_limit: int, bud_would: int, bud_cap: int, mem_hw: int, mem_rt: int, admin: str, resolve: str, resolve_addr: str, resolve_timeout_ms: int, resolve_valid_ms: int }
    [36] pub ServerOpts — struct ServerOpts { token: str, keepalive_timeout_ms: int, keepalive_requests: int, read_timeout_ms: int, challenge_dir: str, metrics_on: bool, pipe_w: int, pipe_r: int }

