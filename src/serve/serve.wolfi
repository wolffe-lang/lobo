module pkg :: serve
  wolfi v0 · toolchain 0.2.17 · edition v1
  export_hash f9709babc9eb140d15934b85fa4b493cf86722830c8ad1f776345f0b5289f0f7
  pkg_hash    f9709babc9eb140d15934b85fa4b493cf86722830c8ad1f776345f0b5289f0f7
  deps:
    budget  9299e00a4af3ebec20e314be715603c6490f169af63de01461fb714934914390
    config  9ed939f614ce6af74036fc5df0e4e12bc6ffb495d284a298c24151b91af1b69d
    conn  19d104d333af9d3379c1133c85b4da87fe9940218314179796e23ea724872638
    http  c4ae23a48d83a4eb91660743c9297b43ff41762f954bcf4da45d99304c06fbb0
    metrics  d7876b1d15ed33fdcab7ea69533f1c19ce89efde7c2fb1ac340b777fed472ea1
    obs  f1404aeee2cc681b8063c87ce5083327b1c70d50a99255aeac7314f2d833d0aa
    proxy  3b42ba71d27675a885226fde09eae1d0544de7d5fd6b0b4e98cbaee1525b4ac9
    resolver  915b57fb63a5793ef1de62249bc2545d4c9839763f6827c25b58f0a7a42ca4c1
    std.range  d7c8e2cafbb418b680a777f06602e91128373f7ed3b341a20edccaa108e513bf
  items:
    [0] pub accept_loop — fn accept_loop(c: config.Conf, addr: str, opts: self.ServerOpts, limits: http.Limits) -> () ! {io} · regions (ρ1, -, ρ2, ρ3) -> -
    [1] pub admin_err_len — fn admin_err_len(token: str) -> int
    [2] pub admin_kind — fn admin_kind(on: bool, path: str) -> str
    [3] pub carry_has_head — fn carry_has_head(carry: str) -> bool
    [4] pub classify — fn classify(path: str) -> self.Classified · regions (-) -> ρ_caller
    [5] pub conn_idle — fn conn_idle(carry: str, served: int) -> self.ConnStep · regions (-, -) -> ρ_caller
    [6] pub conn_step — fn conn_step(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, carry: str, served: int, poll_ms: int, mut rs: resolver.Resolver, mut ft: self.FileKinds) -> self.ConnStep · regions (-, ρ1, ρ2, ρ3, -, -, -, -, ρ4, ρ5) -> ρ_caller
    [7] pub date_str — fn date_str(mut ft: self.FileKinds, secs: int) -> str · regions (ρ1, -) -> -
    [8] pub default_opts — fn default_opts() -> self.ServerOpts · regions () -> ρ_caller
    [9] pub error_body — fn error_body(code: int, server: str) -> str
    [10] pub error_response — fn error_response(code: int, special_header_body: bool, date: str, token: str) -> str
    [11] pub file_kinds_new — fn file_kinds_new() -> self.FileKinds · regions () -> ρ_caller
    [12] pub head_remember — fn head_remember(mut ft: self.FileKinds, path: str, size: int, mtime_ms: int, keep: bool, secs: int, token: str, head: str) · regions (ρ1, -, -, -, -, -, -, -) -> -
    [13] pub head_warm — fn head_warm(ft: self.FileKinds, path: str, size: int, mtime_ms: int, keep: bool, secs: int, token: str) -> str · regions (ρ1, -, -, -, -, -, -) -> -
    [14] pub header_too_large_body — fn header_too_large_body(server: str) -> str
    [15] pub http_date — fn http_date(secs: int) -> str
    [16] pub ledger_cap — fn ledger_cap(budget: int) -> int
    [17] pub listen_addr — fn listen_addr(c: config.Conf) -> str · regions (ρ1) -> -
    [18] pub listen_addr_plain — fn listen_addr_plain(c: config.Conf) -> str · regions (ρ1) -> -
    [19] pub listen_reuseport — fn listen_reuseport(c: config.Conf) -> bool · regions (ρ1) -> -
    [20] pub mime_for — fn mime_for(c: config.Conf, path: str) -> str · regions (ρ1, -) -> -
    [21] pub next_boundary — fn next_boundary(mut ft: self.FileKinds) -> str · regions (ρ1) -> -
    [22] pub open_self_pipe — fn open_self_pipe() -> (int, int) ! {io} · regions () -> ρ_caller
    [23] pub range_plan — fn range_plan(spec: str, if_range: str, etag: str, lm: str, size: int, max_ranges: int) -> self.RangePlan · regions (-, -, -, -, -, -) -> ρ_caller
    [24] pub raw_line — fn raw_line(head: str) -> str
    [25] pub resolve_limits — fn resolve_limits(c: config.Conf) -> http.Limits · regions (ρ1) -> ρ_caller
    [26] pub serve_conn — fn serve_conn(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int) · regions (-, ρ1, ρ2, ρ3, -) -> -
    [27] pub serve_conn_cn — fn serve_conn_cn(mut cn: conn.Conn, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, mut rs: resolver.Resolver) · regions (ρ1, ρ2, ρ3, ρ4, -, ρ5) -> -
    [28] pub serve_request — fn serve_request(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, head: str, rest0: str, served: int, mut rs: resolver.Resolver, mut ft: self.FileKinds) -> self.ReqOut · regions (-, ρ1, ρ2, ρ3, -, -, -, -, ρ4, ρ5) -> ρ_caller
    [29] pub status_phrase — fn status_phrase(code: int) -> str
    [30] pub to_hex — fn to_hex(n: int) -> str
    [31] pub version — fn version() -> str
    [32] pub write_admin — fn write_admin(conn: int, body: str, ctype: str, token: str, keep: bool) -> int ! {closed, io}
    [33] pub write_admin_503 — fn write_admin_503(conn: int, token: str)
    [34] pub Classified — struct Classified { kind: int, fd: int, size: int, mtime_ms: int }
    [35] pub ConnStep — struct ConnStep { active: bool, close: bool, carry: str, served: int, logged: bool, acc: obs.Access, bud_site: str, bud_limit: int, bud_would: int, bud_cap: int, mem_hw: int, mem_rt: int, admin: str, resolve: str, resolve_addr: str, resolve_timeout_ms: int, resolve_valid_ms: int }
    [36] pub FileKinds — struct FileKinds { hpath: prelude.List[str], hsize: prelude.List[int], hmtime: prelude.List[int], hsecs: prelude.List[int], hkeep: prelude.List[int], htoken: prelude.List[str], hhead: prelude.List[str], date_secs: int, date_str: str, boundary: int }
    [37] pub RangePlan — struct RangePlan { kind: int, starts: prelude.List[int], ends: prelude.List[int] }
    [38] pub ReqOut — struct ReqOut { close: bool, carry: str, logged: bool, acc: obs.Access, bud_site: str, bud_limit: int, bud_would: int, bud_cap: int, mem_hw: int, mem_rt: int, admin: str, resolve: str, resolve_addr: str, resolve_timeout_ms: int, resolve_valid_ms: int }
    [39] pub ServerOpts — struct ServerOpts { token: str, keepalive_timeout_ms: int, keepalive_requests: int, read_timeout_ms: int, challenge_dir: str, metrics_on: bool, pipe_w: int, pipe_r: int }

