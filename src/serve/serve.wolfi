module pkg :: serve
  wolfi v0 · toolchain 0.2.3 · edition v1
  export_hash bb5428b05851b211acab22d8eb5afbcdc3985c2184a8f3a5cef55f9e9c904d8c
  pkg_hash    bb5428b05851b211acab22d8eb5afbcdc3985c2184a8f3a5cef55f9e9c904d8c
  deps:
    budget  e9419042d06548f1a523738ead044d657fbe591539b2ec1c9e9d01598c5d8d22
    config  4ca8619eed493da02f2439b1eef73db3a5b7b2b39a30e5caaf716938200531ed
    conn  0c954129062114b97cbe662f65c32f66f263fb6189e4a650adce1f050c0142d7
    http  6e18c1b349576e6c7acf4b757cfceb8543558460150de261f1d511946166219e
    metrics  3fa3431c8c908c1e104348f727d2af16668bfcf3de371ed93962b5e029b0ac9d
    obs  5634b6ed2d5b23cd8b20b3c11a1e5ca4a225b1aed8c63d0e3e84136b42fadfe3
    proxy  6fa3467d0251a913446bc6aa7ed6303246c3c420eed2a7a66c1c8ee3692e8b47
    resolver  335a608ed8a1bcaebc89315ca204485dd53b25c5cc15e7b78fd88a4e99e07f2e
  items:
    [0] pub accept_loop — fn accept_loop(c: config.Conf, addr: str, opts: self.ServerOpts, limits: http.Limits) -> () ! {io} · regions (ρ1, -, ρ2, ρ3) -> -
    [1] pub admin_err_len — fn admin_err_len(token: str) -> int
    [2] pub admin_kind — fn admin_kind(on: bool, path: str) -> str
    [3] pub conn_step — fn conn_step(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, carry: str, served: int, poll_ms: int, mut rs: resolver.Resolver) -> self.ConnStep · regions (-, ρ1, ρ2, ρ3, -, -, -, -, ρ4) -> ρ_caller
    [4] pub default_opts — fn default_opts() -> self.ServerOpts · regions () -> ρ_caller
    [5] pub error_body — fn error_body(code: int, server: str) -> str
    [6] pub error_response — fn error_response(code: int, special_header_body: bool, date: str, token: str) -> str
    [7] pub header_too_large_body — fn header_too_large_body(server: str) -> str
    [8] pub http_date — fn http_date(secs: int) -> str
    [9] pub ledger_cap — fn ledger_cap(budget: int) -> int
    [10] pub listen_addr — fn listen_addr(c: config.Conf) -> str · regions (ρ1) -> -
    [11] pub listen_addr_plain — fn listen_addr_plain(c: config.Conf) -> str · regions (ρ1) -> -
    [12] pub mime_for — fn mime_for(c: config.Conf, path: str) -> str · regions (ρ1, -) -> -
    [13] pub open_self_pipe — fn open_self_pipe() -> (int, int) ! {io} · regions () -> ρ_caller
    [14] pub raw_line — fn raw_line(head: str) -> str
    [15] pub resolve_limits — fn resolve_limits(c: config.Conf) -> http.Limits · regions (ρ1) -> ρ_caller
    [16] pub serve_conn — fn serve_conn(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int) · regions (-, ρ1, ρ2, ρ3, -) -> -
    [17] pub serve_conn_cn — fn serve_conn_cn(mut cn: conn.Conn, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, mut rs: resolver.Resolver) · regions (ρ1, ρ2, ρ3, ρ4, -, ρ5) -> -
    [18] pub serve_request — fn serve_request(conn: int, c: config.Conf, opts: self.ServerOpts, limits: http.Limits, local_port: int, head: str, rest0: str, served: int, mut rs: resolver.Resolver) -> self.ReqOut · regions (-, ρ1, ρ2, ρ3, -, -, -, -, ρ4) -> ρ_caller
    [19] pub status_phrase — fn status_phrase(code: int) -> str
    [20] pub to_hex — fn to_hex(n: int) -> str
    [21] pub version — fn version() -> str
    [22] pub write_admin — fn write_admin(conn: int, body: str, ctype: str, token: str, keep: bool) -> int ! {closed, io}
    [23] pub write_admin_503 — fn write_admin_503(conn: int, token: str)
    [24] pub ConnStep — struct ConnStep { active: bool, close: bool, carry: str, served: int, logged: bool, acc: obs.Access, bud_site: str, bud_limit: int, bud_would: int, bud_cap: int, mem_hw: int, mem_rt: int, admin: str, resolve: str, resolve_addr: str, resolve_timeout_ms: int, resolve_valid_ms: int }
    [25] pub ReqOut — struct ReqOut { close: bool, carry: str, logged: bool, acc: obs.Access, bud_site: str, bud_limit: int, bud_would: int, bud_cap: int, mem_hw: int, mem_rt: int, admin: str, resolve: str, resolve_addr: str, resolve_timeout_ms: int, resolve_valid_ms: int }
    [26] pub ServerOpts — struct ServerOpts { token: str, keepalive_timeout_ms: int, keepalive_requests: int, read_timeout_ms: int, challenge_dir: str, metrics_on: bool, pipe_w: int, pipe_r: int }

