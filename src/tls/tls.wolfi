module pkg :: tls
  wolfi v0 · toolchain 0.2.5 · edition v1
  export_hash 686769bf96779e85587e8bc64281466e47cd6ac0db6e6ee40fe52157b67b8049
  pkg_hash    686769bf96779e85587e8bc64281466e47cd6ac0db6e6ee40fe52157b67b8049
  deps:
    config  e5c956488d6a43bae972cb1d7fdd3e839dfbc6cec16a28a4b0a4a04c74426691
    std.x.crypto.curve25519  3acb8e31dae920b175115428b6b9a4b712a7812135125b4dd3510d1e960fb310
    std.x.tls.cert  efc0e562f119b26b3f82aebb6ab7a8297ace32bdcd4ade4d4ff4abdab464583f
    std.x.tls.handshake  69bfdce700ac9c051b7a05eba216d38c4f00d3b1ca64806de3b8968cbaacc355
    std.x.tls.record  95a1128f78d70821ffc8657908fcfd09b66890ebd8460427ec6c45f6ba8866a5
  items:
    [0] pub accept — fn accept(fd: int, sc: config.SslConf, deadline_ms: int) -> self.Sess ! {io, refused, timeout} · regions (-, ρ1, -) -> ρ_caller
    [1] pub alert_name — fn alert_name(code: int) -> str
    [2] pub no_sess — fn no_sess() -> self.Sess · regions () -> ρ_caller
    [3] pub parse_client_hello — fn parse_client_hello(msg: prelude.List[byte]) -> self.Hello · regions (ρ1) -> ρ_caller
    [4] pub sess_close — fn sess_close(mut z: self.Sess) · regions (ρ1) -> -
    [5] pub sess_read — fn sess_read(mut z: self.Sess, max: int) -> prelude.List[byte] ! {closed, io, timeout} · regions (ρ1, -) -> ρ_caller
    [6] pub sess_write — fn sess_write(mut z: self.Sess, data: prelude.List[byte]) -> () ! {closed, io} · regions (ρ1, ρ2) -> -
    [7] pub version — fn version() -> str
    [8] pub Hello — struct Hello { ok: bool, alert: int, reason: str, random: prelude.List[byte], session_id: prelude.List[byte], has_chacha: bool, has_13: bool, has_ed25519: bool, kx: prelude.List[byte], sni: str, alpn_offered: bool, alpn_http11: bool }
    [9] pub Sess — struct Sess { fd: int, up: bool, rkey: prelude.List[byte], riv: prelude.List[byte], rseq: int, wkey: prelude.List[byte], wiv: prelude.List[byte], wseq: int, buf: prelude.List[byte], plain: prelude.List[byte], got_close: bool, sent_close: bool, trunc: bool, sni: str, alpn: str, entry: int }

