//! member: true
//! config — nginx.conf loading: lexer, parser, include graph, and the
//! directive tables. ws01's territory; a stub at ws00 so the `.wolfi`
//! interface machinery has a real surface to freeze. Do not grow this
//! module outside the ws01 contract.

/// This module's interface version. Frozen by the ws00 `.wolfi`
/// snapshot beside this file; ws01 bumps it with its first real
/// surface. Total; never traps.
#[allow(w0314)]
pub fn version() -> str {
    "0.0.1"
}
