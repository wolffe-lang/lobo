module pkg :: proxy
  wolfi v0 · toolchain 0.2.5 · edition v1
  export_hash 1a47f365f3dadc5b0a7317f1b1be3db2d89a3ffca19d25586b5d497cc4a79936
  pkg_hash    1a47f365f3dadc5b0a7317f1b1be3db2d89a3ffca19d25586b5d497cc4a79936
  deps:
    config  e5c956488d6a43bae972cb1d7fdd3e839dfbc6cec16a28a4b0a4a04c74426691
    http  4679240b1a911cb17a24ab7e1ba811b21af1e4b847ee54ad0b893449e662da11
    resolver  65320cc9c063edfd1d24c75a7ec32add2ededfd4377762251c1a50d4d24ddc67
    std.net  67bf01be88fa2c2250c1f5f73bce68e9e80c23fc02a1a6888d934fd1ad765155
    std.x.tls.cert  efc0e562f119b26b3f82aebb6ab7a8297ace32bdcd4ade4d4ff4abdab464583f
    std.x.tls.client  aaf8f048ae92465909429bcc6d8db057d8624a67102df90ce62cc83ca50a60cf
  items:
    [0] pub build_upstream — fn build_upstream(c: config.Conf, req: http.Request, pl: self.Plan, phost: str, pport: int, remote_addr: str, body: str) -> str · regions (ρ1, ρ2, ρ3, -, -, -, -) -> -
    [1] pub expand_var — fn expand_var(v: str, host: str, remote_addr: str, proxy_host: str, inbound_xff: str) -> str
    [2] pub is_hop_by_hop — fn is_hop_by_hop(lname: str) -> bool
    [3] pub parse_pass — fn parse_pass(c: config.Conf, arg: str) -> self.PassTarget · regions (ρ1, -) -> ρ_caller
    [4] pub parse_response — fn parse_response(head: str) -> self.UpResponse · regions (-) -> ρ_caller
    [5] pub peer_count — fn peer_count(p: self.UpPool) -> int · regions (ρ1) -> -
    [6] pub peer_down — fn peer_down(fails: int, last_fail_ms: int, max_fails: int, fail_ms: int, now_ms: int) -> bool
    [7] pub plan — fn plan(c: config.Conf, path: str) -> self.Plan · regions (ρ1, -) -> ρ_caller
    [8] pub pool_named — fn pool_named(c: config.Conf, name: str) -> self.UpPool · regions (ρ1, -) -> ρ_caller
    [9] pub relay_response — fn relay_response(up: self.UpResponse, body: str, token: str, date: str, keep: bool) -> str · regions (ρ1, -, -, -, -) -> -
    [10] pub resolve_need — fn resolve_need(c: config.Conf, path: str, rs: resolver.Resolver, now: int) -> self.Need · regions (ρ1, -, ρ2, -) -> ρ_caller
    [11] pub rewrite_target — fn rewrite_target(matched: str, has_uri: bool, uri: str, path: str, query: str) -> str
    [12] pub run — fn run(client: int, c: config.Conf, req: http.Request, body: str, pl: self.Plan, remote_addr: str, token: str, date: str, keep: bool, mut rs: resolver.Resolver, mut acc: self.RunAcc) -> bool ! {closed, io} · regions (-, ρ1, ρ2, -, ρ3, -, -, -, -, ρ4, ρ5) -> -
    [13] pub run_acc — fn run_acc() -> self.RunAcc · regions () -> ρ_caller
    [14] pub set_headers — fn set_headers(c: config.Conf, loc: int, srv: int, http_blk: int) -> (prelude.List[str], prelude.List[str]) · regions (ρ1, -, -, -) -> ρ_caller
    [15] pub status_code — fn status_code(status_line: str) -> int
    [16] pub status_code_of — fn status_code_of(status_line: str) -> int
    [17] pub swrr_new — fn swrr_new(n: int) -> self.Swrr · regions (-) -> ρ_caller
    [18] pub swrr_pick — fn swrr_pick(mut s: self.Swrr, weights: prelude.List[int], up: prelude.List[bool]) -> int · regions (ρ1, ρ2, ρ3) -> -
    [19] pub version — fn version() -> str
    [20] pub Need — struct Need { host: str, addr: str, timeout_ms: int, valid_ms: int }
    [21] pub PassTarget — struct PassTarget { scheme: str, authority: str, is_pool: bool, has_uri: bool, uri: str, err: str }
    [22] pub Plan — struct Plan { is_proxy: bool, loc: int, srv: int, http_blk: int, matched: str, pt: self.PassTarget, connect_ms: int, read_ms: int, trust_path: str, ssl_name: str, resolver: str, resolver_valid_ms: int, resolver_timeout_ms: int }
    [23] pub RunAcc — struct RunAcc { addr: str, status: int, body_bytes: int, up_ms: int }
    [24] pub Swrr — struct Swrr { current: prelude.List[int] }
    [25] pub UpPool — struct UpPool { name: str, hosts: prelude.List[str], ports: prelude.List[int], weights: prelude.List[int], max_fails: prelude.List[int], fail_ms: prelude.List[int], backup: prelude.List[bool], rows: prelude.List[int] }
    [26] pub UpResponse — struct UpResponse { status_line: str, hnames: prelude.List[str], hvals: prelude.List[str], body_mode: str, content_length: int }

