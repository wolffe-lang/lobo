module pkg :: proxy
  wolfi v0 · toolchain 0.2.0 · edition v1
  export_hash def690ff64fc7edb3d3bc09c15a3fbc02525a70a1ef00e3d1c0078d16f919987
  pkg_hash    def690ff64fc7edb3d3bc09c15a3fbc02525a70a1ef00e3d1c0078d16f919987
  deps:
    config  05fa389d384471aeaf3e9597692b9fc9cc5bf3d8c160cc31991c33ee86185e95
    http  757b4fe5402e1ad162ae6903d52a31921a49edebd14f2b79872b164db2fc27de
    std.net  cb2d7259c1f637a4d2c6fb9509f6386d052b5b51bf904aec83719f92cdb1eafb
    std.x.tls.cert  3fe950eea668027b4873aba4519a623def60df3cfee04b0691e5ab02a73622c3
    std.x.tls.client  4f17326ab09ec26a7c250a4878f3a6ab54f317c5ffbc3af40432f8feeb4c565e
  items:
    [0] pub build_upstream — fn build_upstream(c: config.Conf, req: http.Request, pl: self.Plan, phost: str, pport: int, remote_addr: str, body: str) -> str · regions (ρ1, ρ2, ρ3, -, -, -, -) -> -
    [1] pub expand_var — fn expand_var(v: str, host: str, remote_addr: str, proxy_host: str, inbound_xff: str) -> str
    [2] pub is_hop_by_hop — fn is_hop_by_hop(lname: str) -> bool
    [3] pub parse_pass — fn parse_pass(c: config.Conf, arg: str) -> self.PassTarget · regions (ρ1, -) -> ρ_caller
    [4] pub parse_response — fn parse_response(head: str) -> self.UpResponse · regions (-) -> ρ_caller
    [5] pub peer_count — fn peer_count(p: self.UpPool) -> int · regions (ρ1) -> -
    [6] pub peer_down — fn peer_down(fails: int, last_fail_ms: int, max_fails: int, fail_ms: int, now_ms: int) -> bool
    [7] pub plan — fn plan(c: config.Conf, path: str) -> self.Plan · regions (ρ1, -) -> ρ_caller
    [8] pub pool_named — fn pool_named(c: config.Conf, name: str) -> self.UpPool · regions (ρ1, -) -> ρ_caller
    [9] pub relay_response — fn relay_response(up: self.UpResponse, body: str, token: str, date: str, keep: bool) -> str · regions (ρ1, -, -, -, -) -> -
    [10] pub rewrite_target — fn rewrite_target(matched: str, has_uri: bool, uri: str, path: str, query: str) -> str
    [11] pub run — fn run(client: int, c: config.Conf, req: http.Request, body: str, pl: self.Plan, remote_addr: str, token: str, date: str, keep: bool, mut acc: self.RunAcc) -> bool ! {closed, io} · regions (-, ρ1, ρ2, -, ρ3, -, -, -, -, ρ4) -> -
    [12] pub run_acc — fn run_acc() -> self.RunAcc · regions () -> ρ_caller
    [13] pub set_headers — fn set_headers(c: config.Conf, loc: int, srv: int, http_blk: int) -> (prelude.List[str], prelude.List[str]) · regions (ρ1, -, -, -) -> ρ_caller
    [14] pub status_code — fn status_code(status_line: str) -> int
    [15] pub status_code_of — fn status_code_of(status_line: str) -> int
    [16] pub swrr_new — fn swrr_new(n: int) -> self.Swrr · regions (-) -> ρ_caller
    [17] pub swrr_pick — fn swrr_pick(mut s: self.Swrr, weights: prelude.List[int], up: prelude.List[bool]) -> int · regions (ρ1, ρ2, ρ3) -> -
    [18] pub version — fn version() -> str
    [19] pub PassTarget — struct PassTarget { scheme: str, authority: str, is_pool: bool, has_uri: bool, uri: str, err: str }
    [20] pub Plan — struct Plan { is_proxy: bool, loc: int, srv: int, http_blk: int, matched: str, pt: self.PassTarget, connect_ms: int, read_ms: int, trust_path: str, ssl_name: str }
    [21] pub RunAcc — struct RunAcc { addr: str, status: int, body_bytes: int, up_ms: int }
    [22] pub Swrr — struct Swrr { current: prelude.List[int] }
    [23] pub UpPool — struct UpPool { name: str, hosts: prelude.List[str], ports: prelude.List[int], weights: prelude.List[int], max_fails: prelude.List[int], fail_ms: prelude.List[int], backup: prelude.List[bool] }
    [24] pub UpResponse — struct UpResponse { status_line: str, hnames: prelude.List[str], hvals: prelude.List[str], body_mode: str, content_length: int }

