module pkg :: shell
  wolfi v0 · toolchain 0.1.0 · edition v1
  export_hash d5a4e9cca192ad8452683f845917a8cbe215d891a44cbff5c0f737df33b8a4fd
  pkg_hash    d5a4e9cca192ad8452683f845917a8cbe215d891a44cbff5c0f737df33b8a4fd
  deps:
    config  46e40ce27c442fc3257f65b51ccf07dcddfcb3b2e26edada020817dd00efcaec
  items:
    [0] pub control_addr — fn control_addr(c: config.Conf) -> str · regions (ρ1) -> -
    [1] pub control_alive — fn control_alive(addr: str) -> bool
    [2] pub control_send — fn control_send(addr: str, line: str) -> str ! {io, refused}
    [3] pub gen_new — fn gen_new(c: config.Conf) -> self.Gen · regions (ρ1) -> ρ_caller
    [4] pub listen_added — fn listen_added(old: prelude.List[str], new: prelude.List[str]) -> prelude.List[str] · regions (ρ1, ρ2) -> ρ_caller
    [5] pub listen_addrs — fn listen_addrs(c: config.Conf) -> prelude.List[str] · regions (ρ1) -> ρ_caller
    [6] pub listen_removed — fn listen_removed(old: prelude.List[str], new: prelude.List[str]) -> prelude.List[str] · regions (ρ1, ρ2) -> ρ_caller
    [7] pub listen_reused — fn listen_reused(old: prelude.List[str], new: prelude.List[str]) -> prelude.List[str] · regions (ρ1, ρ2) -> ρ_caller
    [8] pub parse_cli — fn parse_cli(args: prelude.List[str]) -> self.Cli · regions (ρ1) -> ρ_caller
    [9] pub pid_path — fn pid_path(c: config.Conf, prefix: str) -> str · regions (ρ1, -) -> -
    [10] pub pid_read — fn pid_read(path: str) -> str ! {io, none}
    [11] pub pid_remove — fn pid_remove(path: str) -> () ! {io}
    [12] pub pid_write — fn pid_write(path: str, endpoint: str) -> () ! {io}
    [13] pub reload — fn reload(prefix: str, conf_rel: str, prev: self.Gen) -> self.Gen · regions (-, -, ρ1) -> ρ_caller
    [14] pub signal_ok — fn signal_ok(name: str) -> bool
    [15] pub signal_unix — fn signal_unix(name: str) -> str
    [16] pub usage_text — fn usage_text() -> str
    [17] pub version — fn version() -> str
    [18] pub version_report — fn version_report() -> str
    [19] pub version_token — fn version_token() -> str
    [20] pub version_verbose — fn version_verbose() -> str
    [21] pub Cli — struct Cli { mode: str, signal: str, prefix: str, conf: str, errlog: str, globals: str, quiet: bool, err_msg: str, exit_code: int }
    [22] pub Gen — struct Gen { conf: config.Conf, gen: int, ok: bool, note: str }

